c     programa 1-1
      write(*,*) 'Insira r1, r2'
      read(*,*) ar1, ar2
      pi = acos(-1.e0)
      write(*,*) 'pi:', pi
      a_area = 2 * pi * r2 * 2 * pi * r1
      write(*,*) 'área:', a_area
      end
